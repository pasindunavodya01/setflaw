# SetFlow

A lightweight PWA to track workouts and progressive overload. Built with React + Supabase.

Getting started:

1. Copy `.env.example` to `.env` and fill your Supabase credentials.
2. Install dependencies: `npm install`
3. Start dev server: `npm run dev`

This repo contains a minimal starter scaffold (PWA manifest, service worker, Supabase client).